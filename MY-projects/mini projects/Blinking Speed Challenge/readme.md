Connect one LED and one button.
LED blinks automatically.
Each button press increases the blinking speed.
If the blinking interval becomes too fast (<100 ms), the LED turns OFF permanently (loss condition).
